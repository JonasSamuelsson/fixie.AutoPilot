<Window x:Class="Fixie.AutoPilot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:autoPilot="clr-namespace:Fixie.AutoPilot"
        Title="fixie AutoPilot" SizeToContent="Height" ResizeMode="CanMinimize" Width="500">
	<Window.DataContext>
		<autoPilot:MainWindowViewModel/>
	</Window.DataContext>
	<Grid>
		<Grid Visibility="{Binding Setup.Visible.Value, Converter={x:Static autoPilot:Bool2VisibilityConverter.Instance}}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition/>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition Width="Auto"/>
			</Grid.ColumnDefinitions>
			<TextBox Text="{Binding Setup.Solution.Value, UpdateSourceTrigger=PropertyChanged}"/>
			<Button Grid.Column="1" Content="..." Command="{Binding Setup.BrowseCommand}" />
			<Button Grid.Column="2" Content="GO" Command="{Binding Setup.GoCommand}" />
		</Grid>
		<Grid Visibility="{Binding Content.Visible.Value, Converter={x:Static autoPilot:Bool2VisibilityConverter.Instance}}">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>
			<ProgressBar Height="3" IsIndeterminate="True"/>
			<TextBox Grid.Row="1" IsReadOnly="True" Height="250" Text="{Binding Content.Text.Value}" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>
		</Grid>
	</Grid>
</Window>
